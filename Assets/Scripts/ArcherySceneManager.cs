using UnityEngine;

public class ArcherySceneManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    private Vector3 position = new Vector3(-16.06f, 1.73f, -4.956f);

    private Vector3 rotation = new Vector3(13.58f, -91.41f, 0);
    // Update is called once per frame
    void Update()
    {
        
    }
}
